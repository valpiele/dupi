namespace Planner.Models;

public enum TicketType
{
    Feature = 0,
    Bug = 1
}

public enum TicketState
{
    Open = 0,
    InProgress = 1,
    Closed = 2
}

public enum MemberRole
{
    Owner = 0,
    Member = 1
}
